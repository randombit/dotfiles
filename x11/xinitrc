#!/bin/sh

makepath=$HOME/conf/bin/makepath
PATH=$($makepath)

xset m 1 0 

xmodmap -e "keycode 166 = Prior"
xmodmap -e "keycode 167 = Next"

xscreensaver -no-splash &

if type -p set_background
then
  set_background
fi

exec sawfish
